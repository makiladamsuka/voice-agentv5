# Python 3.10+ recommended for best compatibility

# LiveKit components (voice agent framework)
livekit==1.1.5
livekit-agents==1.5.2
livekit-plugins-openai==1.5.2
livekit-plugins-deepgram==1.5.2
livekit-plugins-silero==1.5.1

# Computer vision (YuNet faces + YOLOv8n person/body via OpenCV DNN)
opencv-python==4.12.0.88
face-recognition==1.3.0
mediapipe  # Install via pip: pip install mediapipe (Note: on Raspberry Pi 64-bit, pip install is fully supported)


# RAG - OCR and Vector Database
pytesseract
Pillow
chromadb

# Raspberry Pi Camera (install via: sudo apt install python3-picamera2)
# picamera2 is system-specific and typically installed via apt on Raspberry Pi OS

# IMPORTANT: On Raspberry Pi, do NOT pip install numpy!
# Use system numpy via: python3 -m venv --system-site-packages venv
# This avoids version conflicts with picamera2/simplejpeg

# Display and Servo hardware drivers
adafruit-circuitpython-rgb-display
adafruit-circuitpython-servokit
luma.oled
luma.lcd

# Sentiment analysis for emotion sync
vaderSentiment

# Raspberry Pi I2C (BMI160 head IMU) and ESP32 serial
smbus2
pyserial

# Utilities
python-dotenv==1.2.1
duckduckgo-search
pyyaml
openai>=1.0.0
